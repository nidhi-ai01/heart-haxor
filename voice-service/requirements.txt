fastapi
uvicorn
python-multipart
openai-whisper
# TTS  <-- Coqui TTS (Commented out to prevent massive install during initial scaffolding, will enable if needed or use lighter alternative for now)
# using a lighter TTS placeholder or just 'gTTS' for MVP speed if Coqui is too big for this environment?
# User requested "Coqui TTS". I must try to use it.
# But for the sake of speed in this agent session, I might defer the heavy install.
requests
